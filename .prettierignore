node_modules
package-lock.json

# Non-theme folders
.context
.shopify
.github
.vscode
.claude
.scripts
.playwright-mcp

# Colocated element sources — carry forward the .context / .tests exemptions above.
# Specs are prose reference (prettier reflows their tables); test files carry
# hand-crafted formatting. Validation JSON source stays prettier-managed (plain data).
*.spec.md
*.test.js

# Working scaffolding
BACKLOG.md

# Editor-managed JSON — the Shopify theme editor / admin language editor rewrites
# these on customization, so formatting them just fights the editor.
templates/
sections/*.json
config/settings_data.json
config/markets.json
locales/
!locales/en.default.json
