# Claude Code junctions to .context/ (tracked on the context branch)
.claude/rules/
.claude/skills/

# Personal Claude Code permissions
.claude/settings.local.json

# Worktree pinned to the orphan context branch
.context/

# Shopify CLI local state
.shopify/

# Node
node_modules/
package-lock.json

# Environment
.env

# MCP — local instance may carry per-environment paths or future secrets;
# .mcp.template.json is the committed baseline. Copy to .mcp.json on first setup.
.mcp.json

# Playwright MCP — local debug artifacts (snapshots, console logs, screenshots)
.playwright-mcp/

# Playwright e2e — run output (the .tests/ specs themselves are committed)
test-results/
playwright-report/
.cache/

# Generated validation fixture — staged from a colocated *.validation.json source
# into the generic `?view=validation` slot, then dropped after each test run;
# never committed (it must still push). The obsolete hand-maintained hub that
# previously occupied this path was removed so the rule can take effect.
templates/index.validation.json

# Local backlog scaffolding — never committed
BACKLOG.md

# Foundational audit scaffolding — never committed
FOUNDATIONAL-AUDIT.md

# Strategic audit scaffolding + raw agent reports — never committed
STRATEGIC-AUDIT.md
strategic-audit-reports/
