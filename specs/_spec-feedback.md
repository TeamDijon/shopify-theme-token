# Spec-system feedback

Improvement log for the spec template, conventions, and reference voice. Captures per-phase friction the implementer / reviewer / authoring agent encountered against the spec contract, so the spec system can evolve from real shipping signal instead of speculation.

Read at the start of an authoring or review pass when a refresh on recent frictions would help. Appended to whenever a pipeline phase surfaces friction worth recording.

## When to append

Append a row to the **Log** table when a pipeline phase (authoring, review, implementation, validation, audit) surfaces friction the spec system didn't already absorb. Skip when the phase was friction-free — empty rows are noise.

The implementation skill's checklist names this explicitly; other skills should follow the same discipline as they mature.

## Pass enum

| Value | Source skill (pipeline phase) | Typical friction shape |
|---|---|---|
| `author` | spec-author (phase 2) | Template gap (no section/field fits the spec's nature); convention ambiguity surfaced while filling the template |
| `review` | spec-review (phase 3) | Voice drift the reviewer noticed; section ordering that read poorly; cross-reference gaps; convention bullets the spec couldn't apply cleanly |
| `impl-first` | implementation (phase 4, first build) | API ambiguity, edge-case behavior not covered, decision punted to implementer |
| `impl-update` | implementation (phase 4, subsequent build) | Drift between amended spec and prior implementation; spec change unclear on backwards-compat surface |
| `validation` | validation (phase 5) | Validation matrix doesn't translate cleanly into a buildable page; tier sub-shape mismatch; missing assertion vocabulary |
| `audit` | audit (phase 6) | Cross-spec pattern — the only phase scanning multiple specs at once, so its findings tend to be promotion-ready |

Triage (phase 1) doesn't interact with spec content and doesn't write to this log.

## Slug format for candidates

Each `Candidates` cell entry follows `<target>:<short-kebab-description>`:

| Prefix | Target file |
|---|---|
| `template:` | `_template.md` |
| `convention:` | `spec-convention.md` |
| `voice:` | `reference-voice.md` |
| `arch:` | `architecture.md` / `knowledge-architecture.md` / other orientation docs |

Multiple candidates in one row: semicolon-separated.

## Promotion mechanism — manual + periodic

Promotion (a candidate lifts into a real template / convention / voice-rule edit) and demotion (a former rule retires) are **never automatic**. The 2+-occurrence rule used elsewhere in this knowledge layer does not apply here — frequent template churn would force retrofitting across the existing spec corpus, which is expensive.

Periodic review is developer-prompted. Natural moments:

- End of a multi-spec unit of work (e.g., finishing the L0 snippet wave)
- Before invoking a planned audit phase
- When the Promotion candidates section feels crowded enough to act on

During review, candidates get judged on:

- **Recurrence** — has the pattern surfaced 2+ times across reviews?
- **Generality** — does the proposed rule apply broadly (every spec body / every authoring pass / every review close) or only to a niche (specific element type, specific work shape)? **High generality is required for promotion**; niche candidates stay in the table as soft rules.
- **Fit** — does the rule land in an existing doc structure (sibling bullet in `reference-voice.md`, section header in `_template.md`) or require a new artifact?
- **Retrofit cost** — is the lift worth the corpus-wide pass to comply?

Niche but recurring patterns (low generality, high recurrence) are **not failures of the mechanism** — the candidates table itself functions as a soft-rule register that reviewers consult during relevant work. Some candidates legitimately live there indefinitely. Demotion of a promoted rule that proves niche in practice (low fire rate, cognitive cost > clarity gain) is the release valve.

A promotion or demotion lands as its own commit on `context`, with the moved row migrated from Promotion candidates to the matching archived section.

## Log

| Date | Spec | Pass | Commit | Worked | Friction | Tightening | Candidates |
|---|---|---|---|---|---|---|---|
| 2026-06-02 | hydrate v1.0.0 | impl-first | `ce72dc0` | API table unambiguous on defaults + types; validation matrix pinned the IO-constructor arg shape; consumer usage example calibrated real-code shape; "no global registration" line settled the core.js question (with a charitable read) | callback-throw ordering not covered (disconnect-first inferred for safety on the error path); core.js / window.Token re-export decision implied not stated | Implementation-time decisions vs Out of scope overlap (cancellation API + idle variant + interaction variant appear in both); two Behavior bullets describe `IntersectionObserver` platform behavior more than utility contract | `template:throw-path-bullet`; `convention:no-window-token-explicit`; `template:itd-vs-oos-split` |
| 2026-06-02 | dom v2.0.0 | review | `0762eab` | Behavior bullets cover edge cases (lazy on read, null + warn, no creation/mutation) cleanly; cross-refs to `CacheManager` + `base-component` well-placed | Design principle ("stable unique ID per well-known central element") implicit — stated piecewise across Purpose / API table / Out-of-scope rather than led with; reviewer had to surface it via marker | Purpose ¶2 + Behavior bullet "Trim policy" + Future-property-table coda all carried version-history journey residue ("Earlier versions exposed…", "v2.0.0 trim removed 6") — same pattern as events-manager review (marker 3 voice tightening, same day) | `voice:version-history-belongs-in-changelog`; `template:design-principle-upfront-purpose` |
| 2026-06-02 | modifiers-manager v2.0.0 | review | `63ac8c8` | API table comprehensive (constructor + 6 methods documented with params/returns/behavior); cross-references with `modifier-system.md` (JS hooks paragraph + Swapping-a-key's-value rule) interlock cleanly with no drift; Behavior bullets describe semantic invariants ("one value per key") precisely | "No event emission" bullet hedged "uncommon" without naming the underlying reason (mutating component owns the reaction; no listener roundtrip needed) — required marker round-trip to surface; forward-looking upsert deferral lived in a Behavior bullet where readers parse it as current-state availability rather than as a deferred-not-provided note | Out-of-scope missed a design-boundary bullet ("doesn't follow Set/Map/classList overwrite-on-add convention") that earns its keep — the gotcha is documented in Behavior but not surfaced as a deliberate non-feature in OOS; Atomic value swap bullet carried v2.0.0 history ("v2.0.0 explicitly dropped setValue…") — same pattern as events-manager + dom.md | `voice:version-history-belongs-in-changelog`; `template:itd-vs-oos-split` (broader scope — see updated description) |
| 2026-06-02 | cache-manager v2.0.0 | review | `05c19d9` | API table comprehensive (5 methods + private state documented); Behavior section covers TTL semantics + auto-vivification + chainability + error propagation cleanly; sibling-calibration with `modifiers-manager` and `events-manager` evident in the Map-of-Maps shape and `clear()` lifecycle hook | `has`-TTL-blind design rationale was implicit in the Behavior bullet — required marker round-trip to surface the underlying reason (typical access pattern uses `get` with computeFn which is TTL-aware; `has` exists for introspection only); construction-time cost (one Map allocation for the `dom` type) not explicitly named — required induction across the `dom`-pre-initialized bullet + Lifecycle section + Validation construction case to answer "is this really lazy?" | Behavior bullet "No error swallowing" carried "Per the v2.0.0 changelog" preamble; Out-of-scope "Cache invalidation by predicate" carried "v2.0.0 explicitly dropped `removeIf`" history — 2 sites in one spec, same pattern as previous reviews | `voice:version-history-belongs-in-changelog` |
| 2026-06-02 | observers-manager v3.0.0 | review | `420a4cd` | API table cleanly documents the per-type construction differences (resize/intersection/mutation options-on-construct vs options-on-observe); cross-spec analogy with EventsManager is positive and explicit; v3.0.0 drop-the-sugar divergence is documented as productive convergence with CacheManager's no-sugar pattern | Element-disconnect divergence (analog to events-manager's AbortSignal-divergence pattern) only documented in Validation edge case, not surfaced in Behavior — same shape friction as caught in events-manager review; required marker-2 cross-check to flag | 3 v-history sites — Behavior "No error swallowing" ("Per the v2.0.0 changelog…"); Behavior "No sugar methods" ("v3.0.0 dropped…"); Out-of-scope "Per-type sugar methods" ("v3.0.0 explicitly dropped…"). Same pattern, 5th spec in the batch | `voice:version-history-belongs-in-changelog` |
| 2026-06-02 | document-utils v1.0.0 | review | `0a3cfc6` | Singleton pattern correctly applied + justified (three globally-singleton resources); Behavior section covers the scroll-lock + scrollbar-width mechanisms cleanly; cross-references to `modifiers-manager` + sibling managers anchor the document-level counterpart positioning | Internal contradiction between line 74 ("position: fixed or overflow: hidden") and line 129 ("freeze-via-inset, not overflow-hidden") — same spec asserted both that the mechanism was open + that overflow-hidden was rejected. Reader had to pick which one was authoritative | 2 v-history sites — Purpose ¶2 carried "The module was extracted from core.js in core v1.1.0…" (this spec's own history, distinct from cross-spec meta-references we've been leaving); Related core.md entry carried "the v1.1.0 extraction note documents the move from core.js to document-utils.js" | `voice:version-history-belongs-in-changelog` |
| 2026-06-02 | core v1.3.0 | review | `55bb787` | Three-concerns numbered list at Purpose ¶1 (module orchestration + re-exports + namespace setup) cleanly anchors the entry-point's scope; `window.Token` namespace structure is documented as a JS literal — readers can grep the exact shape; Behavior section covers idempotent initializer + auto-run + custom-element-registration-as-side-effect cleanly | Marker-1 question (`theme:ready` vs typed bus) surfaced a structural rationale that wasn't in the Behavior bullet — the chicken-and-egg problem (bus can't dispatch its own readiness) is load-bearing for understanding the choice; was only inferable from the marker-driven discussion, not from the spec text | 2 v-history sites — Purpose ¶2 carried a paragraph of journey residue ("was previously named 'overloaded'… v1.1.0 extracted…") that was BOTH journey-residue AND redundant with the numbered list above (double tightening: drop journey + drop redundancy in one edit); Behavior bullet "`window.Token` is the global namespace" carried "Renamed from `window.theme` in v1.3.0…" preamble | `voice:version-history-belongs-in-changelog` |
| 2026-06-04 | spacing v1.4.0 (impl: `utility--css-variables.liquid` v1.14.0) | review | `4ce987e` (spec) + `587f4d6` (code) | Cascade-driven override design (substrate defaults + metaobject loop in same `:root`) cleanly documented; merchant-px / front-end-rem pattern surfaced through marker round-trip and harmonized to codebase-wide convention; Spec-prefix marker (M1) used effectively to drive a coordinated schema + behavior + code change in one cycle | The "px values, not rem" Behavior bullet (line 104 pre-edit) accurately described the implementation but framed an inconsistency-with-the-codebase as a deliberate design choice — required a Check-prefix marker (M2) to surface the codebase-wide audit (gutter / text-style / block-layout-vars all emit rem; spacing was the lone outlier); the schema Required:yes on `desktop_value` was an unjustified constraint, surfaced only by the Spec-prefix marker (M1) requesting a no-desktop-value example | Defensive-rebuttal framing at line 46 ("Horizon-style ... doesn't accommodate exotic entries") — caught as unmarked observation; reframed to positive-only ("Two-value-per-token allows arbitrary mobile/desktop pairs ... without re-architecting the scale") | `convention:codebase-wide-emission-audit-on-metaobject-review`; `template:schema-required-fields-justify-the-constraint` |
| 2026-06-04 | content-width v1.0.0 (impl: `section.liquid` v1.7.0 + `utility--block-layout-vars.liquid` v1.1.0) | review | `cb2386b` (spec) + `4fca8bc` (code) | Spec-prefix marker (M1) drove a clean px→rem harmonization across 5 files (spec + sibling spec + 2 emitters + validation page); Chat-prefix marker (M3) surfaced a genuinely useful distinction worth adding to Behavior (content-width is the unusual catalog where handle-rename is purely cosmetic because emission is not handle-keyed); new prefix convention worked smoothly in practice | The cross-spec footprint of the px→rem alignment was larger than spacing's: content-width touched a sibling spec (`utility--block-layout-vars.md`) where a Behavior bullet ("content_width stays in px") carried a now-invalid rationale ("gutter-math is px-based" — gutter actually emits rem). Required cross-spec audit beyond the marker scope to catch | "Pre-rename pass" residue at line 125 — caught as unmarked observation, dropped per the recently-promoted `voice:version-history-belongs-in-changelog` rule | `convention:codebase-wide-emission-audit-on-metaobject-review` (+1 recurrence); `process:metaobject-px-rem-harmonization-touches-sibling-utility-specs` |
| 2026-06-04 | button-style v1.0.0 | review | `4f3eb08` | Six Spec/Check/Chat markers applied efficiently under the new prefix convention — no greenlight thrash, direct apply on the Spec-prefix items; Check (M3) drove a contract-symmetry clarification that was load-bearing (every metaobject entry expects a matching CSS rule); Chat (M7) answered in chat without spec change but uncovered an unmarked BACKLOG-residue site for cleanup | Distinctive design principle ("seeded catalog with free-form per-project extension") was implicit in Purpose ¶1 ("fixed handle vocabulary") — required marker round-trip (M1) to surface; line 74 had a factually wrong fallback claim ("falls through to default `solid-primary` appearance") that required marker (M4) + code-reading to correct (actual fallback is the base button rule, not solid-primary) | Two BACKLOG-residue sites (line 110 "BACKLOG-noted 'link L1 block isn't needed' resolution"; line 144 "BACKLOG Bucket C (C9, surfaced from button spec retrofit 2026-05-29)") — first caught by M6, second by unmarked observation paired with M7 chat answer | `voice:backlog-references-as-residue` (new candidate); `template:design-principle-upfront-purpose` (+1 recurrence — now 2 across dom.md + button-style) |
| 2026-06-04 | media-size v1.0.0 | review | `4fec985` | Single Question-prefix marker (M1, "Briefly" modulator) closed in one round-trip — chat answer + no spec change, exactly the shape the prefix signals; new prefix convention paying off as discipline scales | Implementation pin lacked version pins on consumer references (line 10-13) — drift from `spec-convention.md`'s `path/to/file vX.Y.Z (role)` format; required re-reading current snippet versions to fix (no marker would have caught this without standing audit) | BACKLOG residue at line 47 — second occurrence in two consecutive specs (button-style + media-size). The pattern is recurring across the metaobject-spec subcorpus | `voice:backlog-references-as-residue` (+1 recurrence — now 2 across button-style + media-size); `convention:implementation-pin-version-audit` (new candidate) |
| 2026-06-04 | design-constants v1.6 (substrate) | review | `4137b21` | Developer pre-tagged M1 with the existing soft-rule slug (`convention:codebase-wide-emission-audit-on-metaobject-review`) — prefix-as-diagnosis worked, no chat needed to surface the issue; M2 Question prompt drove a useful Out-of-scope tightening that named the actual current override paths (fork-edit + metaobject-upgrade) instead of the speculative "(when surfaced)" wording | Three stale unit claims in one spec — biggest single-spec footprint of the codebase-wide emission audit so far (Behavior bullet × 2 lines 150+152; Validation assertion line 187). The audit's reach extended beyond metaobject specs proper — this is a *substrate* spec describing a metaobject's emission unit, and it carried the same drift. Slug scope is broader than "metaobject-review" suggests | None — adjacent specs (`spacing.md`, `text-style.md`, `content-width.md`, `utility--block-layout-vars.md`) all carried the merchant-px / front-end-rem framing correctly; design-constants was the lone outlier | `convention:codebase-wide-emission-audit-on-metaobject-review` (+1 recurrence — now 3; scope broader than metaobject-review proper) |
| 2026-06-04 | theme-color v1.0.0 | review | `1649d16` | M2 Check-prefix marker (developer's "Have a look at ZAG…") successfully delegated an empirical question; one bash grep against the codebase confirmed the contract was already tight (only `utility--meta-theme-color` re-extracts `hex_code.value`, which is structural and called out in its own snippet) — bullet dropped clean. M1 Spec-prefix marker drove a section-placement tightening (CSS-properties section should only describe what IS emitted; "not -rgb" framing belongs in OOS where it already lived) | Implementation pin drifted 3 versions on `utility--css-variables.liquid` (v1.11.0 → v1.14.0) — contract-neutral changes (gradient opacity, spacing emission add, spacing px→rem) but the pin had no signal to flag it. Would have required a standing pin-audit step to catch proactively — recurrence of `convention:implementation-pin-version-audit` candidate from media-size review | Journey-residue "after the role rename" caught on Behavior bullet line 76 — fourth spec where the recently-promoted `voice:version-history-belongs-in-changelog` rule fires post-promotion. Rule is paying its keep as a standing check | `convention:implementation-pin-version-audit` (+1 recurrence — now 2 across media-size + theme-color) |

## Promotion candidates

Running list, hand-maintained at periodic review time. Deduplicated across log entries — when a candidate slug recurs, bump Recurrence and Last seen rather than adding a new row.

| Candidate | Recurrence | First / last seen | Target file | Proposed edit |
|---|---|---|---|---|
| `template:throw-path-bullet` | 1 | 2026-06-02 / 2026-06-02 | `_template.md` Behavior guidance | Add guidance for the "error-path contract" — when the spec describes a callback the consumer passes in, name what happens when that callback throws and what the utility guarantees on the error path (e.g., fire-once must still hold; cleanup must still run). |
| `convention:no-window-token-explicit` | 1 | 2026-06-02 / 2026-06-02 | `spec-convention.md` Type-specific variants (utility-js row) | Utility-js specs should explicitly state in Out of scope whether the export is re-exported via `core.js` / `window.Token` — don't leave it to inference from "opt-in per consumer" framing. Two valid placements (re-exported alongside utils, ESM-only) should be a deliberate authoring choice. |
| `template:design-principle-upfront-purpose` | 2 | 2026-06-02 / 2026-06-04 | `_template.md` Purpose-section guidance | When the element has a distinctive design principle (e.g., dom.md's "stable unique ID per central element", hydrate's "fire-once + disconnect-on-visible", button-style's "seeded catalog with free-form per-project extension"), Purpose ¶1 should lead with it rather than letting it surface implicitly across Purpose / API / Behavior / Out-of-scope. Reader assembles the contract from the lead sentence instead of by induction. Recurrences: dom.md (2026-06-02 — implicit unique-ID principle); button-style (2026-06-04 — "fixed handle vocabulary" framing implied no extension; M1 surfaced the seeded-vs-extensible distinction). |
| `convention:codebase-wide-emission-audit-on-metaobject-review` | 3 | 2026-06-04 / 2026-06-04 | `spec-review` skill (checklist) or `spec-convention.md` (cross-spec emission-unit audit step) | When reviewing **any spec that mentions an emission unit** (`px` / `rem` / etc.) — metaobject specs proper *and* substrate specs describing metaobject emission — audit against sibling emitters for unit consistency. Surfaced 3× across 2026-06-04 substrate batch: spacing review M2 (metaobject emitted px while every sibling emitted rem); content-width review M1 (same px-vs-rem inconsistency, plus sibling utility-snippet spec carried an outdated rationale); design-constants review M1 (substrate spec carried 3 stale unit claims about spacing emission). Scope broader than original "metaobject-review" framing suggests — applies to substrate specs that describe metaobject emission downstream. Pattern is recurring — strong promotion signal at the next periodic review. |
| `process:metaobject-px-rem-harmonization-touches-sibling-utility-specs` | 1 | 2026-06-04 / 2026-06-04 | `spec-review` skill (cross-spec audit step) | When a metaobject review surfaces a px→rem harmonization, audit sibling utility-snippet specs that pin the same emission file — they may carry outdated rationale text justifying the old behavior. Surfaced in content-width review: `utility--block-layout-vars.md` carried a "content_width stays in px because gutter-math is px-based" Behavior bullet that was both wrong (gutter emits rem) and overdue for cleanup. Standing audit step would catch this proactively. |
| `voice:backlog-references-as-residue` | 2 | 2026-06-04 / 2026-06-04 | `reference-voice.md` Version-history-references rule (recently promoted) | Extend the version-history-references rule to explicitly cover BACKLOG / TODO / triage-doc references in spec bodies. BACKLOG is a transient queue artifact — what's important about a deferred feature is its contract (out of scope, what would activate it), not its current position in the BACKLOG taxonomy. Recurrences: button-style (2 sites in one spec — M6 + unmarked); media-size (1 site — unmarked). Two specs in two consecutive reviews — recurring pattern. Suggest merging with the v-history bullet under a "Transient-artifact references" framing covering both. |
| `convention:implementation-pin-version-audit` | 2 | 2026-06-04 / 2026-06-04 | `spec-review` skill (checklist item) | When closing a spec review, audit the Implementation pin against `spec-convention.md`'s `path/to/file vX.Y.Z (role)` format **and** against the file's actual current version. Two failure modes surfaced: (a) missing version pins on consumer references (media-size review — 3 consumer references lacked version pins entirely); (b) stale version pins where the file has moved forward on contract-neutral changes (theme-color review — pin at v1.11.0, file at v1.14.0, 3-version drift across gradient opacity + spacing emission + px→rem harmonization). No marker would have caught either without a standing audit step. Adding to the spec-review checklist prevents the drift from accumulating. |
| `template:schema-required-fields-justify-the-constraint` | 1 | 2026-06-04 / 2026-06-04 | `_template.md` Schema-section guidance | Schema field tables that mark a field Required: yes should carry a one-line justification (or the constraint stays unexamined). Surfaced in spacing review (M1): `desktop_value` was Required: yes with no rationale; M1's request to support no-desktop-value entries revealed the constraint was unnecessary, and the schema relaxed in v1.14.0 without any contract loss. A standing nudge would prompt authors to question Required: yes at authoring time. |

## Archived (promoted)

Candidates lifted into real edits. Preserves the trail so a future reviewer can trace why a template / convention bullet exists.

| Landed | Candidate | Target file | Commit |
|---|---|---|---|
| 2026-06-02 | `voice:version-history-belongs-in-changelog` | `reference-voice.md` — new bullet under "Avoid" naming version-history references as residue (7 recurrences / 14 sites across substrate-JS wave 2026-06-02) | `c42508e` |
| 2026-06-02 | `template:itd-vs-oos-split` (broadened: OOS is canonical non-features home) | `_template.md` — three section-description tightenings (Behavior: current-state-only clarifier; ITD: choices-made-during-build boundary; OOS: expanded scope description) | `c42508e` |

## Archived (demoted)

Template / convention / voice-rule bullets retired with reason. Rare — most edits accrete rather than retract.

| Retired | Pattern | Reason | Commit |
|---|---|---|---|

## Periodic review notes

Dated entries from the developer's review passes. One row per review pass, even when no promotions or demotions land.

| Date | Promoted | Demoted | Note |
|---|---|---|---|
| 2026-06-02 | `voice:version-history-belongs-in-changelog` (7×14 sites); `template:itd-vs-oos-split` (2 recurrences, broadened framing) | — | First periodic-review pass on the spec-feedback mechanism, run at the end of the substrate-JS review wave (8 specs reviewed in one day). Three singleton candidates (`template:throw-path-bullet`, `convention:no-window-token-explicit`, `template:design-principle-upfront-purpose`) defer to the next pass — too thin to promote on one occurrence. Both promotions landed as `c42508e` on context. |
